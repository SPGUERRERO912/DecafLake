# DecafLake
A lightweight, simplified implementation inspired by Delta Lake, built in Java to explore how transactional data lakes work under the hood. It focuses on core concepts such as append-only transaction logs, versioned snapshots, time travel queries, schema enforcement, and file compaction without the complexity of distributed infrastructure.
